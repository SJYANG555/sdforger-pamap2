# CONTEXT.md

- 2026-05-02: GPT-2 baseline results are already present on Triton under `outputs/checkpoints/gpt2`, `outputs/generated/gpt2`, `outputs/evaluation/*/gpt2`, and `outputs/plots/gpt2`.
- 2026-05-02: Gemma 2 2B LoRA checkpoint is present at `outputs/checkpoints/gemma/best`.
- 2026-05-02: Gemma generation job `17586805` completed but produced `num_generated_embeddings=0`; all candidates were truncated at 512 new tokens and missed `value_52`, `value_53`.
- 2026-05-02: Increased Gemma `generation.max_new_tokens` from `512` to `640` in `config/pamap2_sdforger_gemma2_2b.yaml`.
- 2026-05-02: Resubmitted Gemma generation as GPU Slurm job `17587441`; completed with `num_generated_embeddings=227`, `duplicate_ratio=0.0`, activities `sitting`, `standing`.
- 2026-05-02: Submitted Gemma CPU evaluation as Slurm job `17588376`; completed successfully with similarity/utility metrics and plots under `outputs/evaluation/*/gemma` and `outputs/plots/gemma`.
- 2026-05-02: Added five-class configs/slurm scripts with `max_prompts=null` and separate `_5class` output directories to preserve two-class results.
- 2026-05-02: GPT-2 five-class generation job `17589303` completed with `856/857` generated embeddings across `cycling`, `running`, `sitting`, `standing`, `walking`; evaluation job `17589898` completed. Utility: synthetic-only accuracy `0.3046`, real+synthetic accuracy `0.9242`.
- 2026-05-02: Gemma five-class generation job `17589315` completed with `636/857` generated embeddings across all five activities; evaluation job `17591549` completed. Utility: synthetic-only accuracy `0.3466`, real+synthetic accuracy `0.9148`.
- 2026-05-02: Added `scripts/plot_5class_model_comparison.py` and generated SDForger-style model comparison plots under `outputs/plots/model_comparison_5class/`.
- 2026-05-03: Added `scripts/evaluate_knn_condition_consistency.py` to mirror the SDForger text-conditioned k-NN consistency check. Results under `outputs/evaluation/knn_condition_5class`: real-test accuracy `0.7783`, GPT-2 synthetic accuracy `0.2173`, Gemma synthetic accuracy `0.3538`.
- 2026-05-03: Added full v2 scaffold without submitting heavy jobs: 108-value FastICA embeddings (`n_components=6`), explicit train-channel standardization, structured v2 text template/parser, `_v2` configs, and v2 Slurm scripts for dataset, training, generation, evaluation, k-NN, and comparison outputs.
- 2026-05-03: Built v2 dataset with CPU Slurm job `17596277` under `artifacts/pamap2_sdforger_dataset_v2`: train/val/test rows `4411/649/857`, all five activities, `108` embedding values, `reducer.pkl`, and `channel_scaler.json`. One non-fatal FastICA convergence warning remained.
- 2026-05-03: Submitted v2 GPU training jobs after dataset validation: Gemma `17596310` (`gemma_v2`) and GPT-2 `17596311` (`gpt2_v2`). Both were pending at submission check.
- 2026-05-03: v2 training completed successfully: Gemma `17596310` completed in `04:09:27`, GPT-2 `17596311` completed in `01:14:35`; both produced `outputs/checkpoints/*_5class_v2/best` and `training_summary.json`.
- 2026-05-03: Submitted v2 GPU generation jobs: GPT-2 `17611524` (`gpt2_gen_v2`) and Gemma `17611525` (`gemma_gen_v2`). Both were pending at submission check.
- 2026-05-03: GPT-2 v2 generation job `17611524` completed but produced `0/857` valid embeddings because the structured named-value target hit GPT-2's 1024 context limit and truncated around `value_99`-`value_102`; Gemma generation `17611525` was still running.
- 2026-05-03: Added compact GPT-2 v2 template/config/artifacts to avoid touching Gemma v2 reducer while Gemma generation runs. Built compact dataset with CPU job `17611628` under `artifacts/pamap2_sdforger_dataset_gpt2_compact_v2` (`108` values, five activities), then submitted compact GPT-2 GPU training job `17613438` (`gpt2c_v2`).
- 2026-05-03: Compact GPT-2 training job `17613438` failed at step `1400` while saving optimizer because `/scratch` was full, but complete checkpoint `checkpoint-1300` was available. Cleaned only failed compact GPT-2 intermediate optimizer/partial checkpoint files to free space, preserving `checkpoint-1300` model/tokenizer, then submitted compact GPT-2 generation job `17619815` using `checkpoint-1300` and output `outputs/generated/gpt2_5class_v2_compact_ckpt1300`. Gemma generation `17611525` was still running.
- 2026-05-03: Compact GPT-2 generation job `17619815` completed, initially all rows failed norm filtering because parser did not recognize `Target values:` and included prompt numbers. Fixed parser, reparsed existing raw generations on CPU to `outputs/generated/gpt2_5class_v2_compact_ckpt1300_reparsed`: `835/857` valid embeddings, all five activities, no duplicates. Submitted compact GPT-2 CPU evaluation job `17620155`; Gemma generation `17611525` was still running.
- 2026-05-03: Compact GPT-2 CPU evaluation job `17620155` completed. Utility: real-only accuracy `0.9148`, synthetic-only `0.4877`, real+synthetic `0.9312`. Similarity overall mean: MDD `0.8614`, ACD `0.3092`, SD `0.01545`, KD `50.0218`, ED `235.5080`, DTW `338.6882`. Gemma generation `17611525` was still running.
- 2026-05-03: Gemma v2 generation job `17611525` failed only at final `generated_windows.npy` save because `/scratch` was full; `generated_embeddings.csv` was complete with `857/857` rows and all five activities. Freed space by deleting only redundant compact GPT-2 intermediate checkpoints and the partial 128-byte Gemma window file, reconstructed Gemma windows from embeddings/reducer, wrote recovered summaries, and submitted Gemma CPU evaluation job `17621571`.
- 2026-05-03: Gemma v2 CPU evaluation job `17621571` completed. Utility: real-only `0.9148`, synthetic-only `0.3501`, real+synthetic `0.9125`. Similarity overall mean: MDD `0.8114`, ACD `0.2868`, SD `0.01395`, KD `52.2752`, ED `183.7506`, DTW `282.1363`. Plots under `outputs/plots/gemma_5class_v2`.
- 2026-05-03: Final v2 compact comparison completed: k-NN job `17621646`, comparison job `17621645`. k-NN condition accuracy: real-test `0.7783`, compact GPT-2 `0.6599`, Gemma `0.6348`. Comparison plots under `outputs/plots/model_comparison_5class_v2_compact`.
